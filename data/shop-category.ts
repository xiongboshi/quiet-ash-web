/**
 * @deprecated Import from `@/data/shop-catalog` or `@/lib/shop-catalog` for new code.
 * Kept so existing imports of `shopCategory` keep working (default incense aisle).
 */
import { shopCatalog } from "@/data/shop-catalog";

export const shopCategory = shopCatalog.incense;
