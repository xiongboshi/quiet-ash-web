/**
 * Which `content/essays/{series}/` folders feed `/journal` index cards.
 * Add a series slug here after `content/series/{slug}.mdx` exists.
 */
export const journalIndexSeriesSlugs = [
  "incense-culture",
] as const satisfies readonly string[];
