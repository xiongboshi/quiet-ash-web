import type { LucideProps } from "lucide-react";

/** Topic hub + evergreen guide — accordion chevron. */
export const JOURNAL_EDITORIAL_FAQ_CHEVRON: LucideProps = {
  size: 15,
  strokeWidth: 1.5,
  absoluteStrokeWidth: true,
  "aria-hidden": true,
};

/** Save / share bar — bookmark. */
export const JOURNAL_EDITORIAL_SAVE_ICON: LucideProps = {
  size: 18,
  strokeWidth: 1.35,
  absoluteStrokeWidth: true,
  "aria-hidden": true,
};

/** Save / share bar — mail & link. */
export const JOURNAL_EDITORIAL_SHARE_ICON: LucideProps = {
  size: 15,
  strokeWidth: 1.5,
  absoluteStrokeWidth: true,
  "aria-hidden": true,
};

/** Evergreen guide — In This Guide nav. */
export const JOURNAL_EDITORIAL_TOC_ICON: LucideProps = {
  size: 16,
  strokeWidth: 1.35,
  absoluteStrokeWidth: true,
  "aria-hidden": true,
};

/** Evergreen guide — scent card badge. */
export const JOURNAL_EDITORIAL_SCENT_ICON: LucideProps = {
  size: 13,
  strokeWidth: 1.4,
  absoluteStrokeWidth: true,
  "aria-hidden": true,
};

/** Evergreen guide — Quick Answer. */
export const JOURNAL_EDITORIAL_QUICK_ICON: LucideProps = {
  size: 16,
  strokeWidth: 1.25,
  absoluteStrokeWidth: true,
  "aria-hidden": true,
};

/** Evergreen guide — feature checklist. */
export const JOURNAL_EDITORIAL_CHECK_ICON: LucideProps = {
  size: 15,
  strokeWidth: 2.25,
  absoluteStrokeWidth: true,
  "aria-hidden": true,
};
